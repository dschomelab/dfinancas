# Project Rules — Finance App

You are modifying an EXISTING production-style codebase exported from Lovable.

Strict rules:

- NEVER recreate the project
- NEVER change framework or stack
- NEVER rewrite architecture unless explicitly requested
- NEVER replace working files unnecessarily
- ALWAYS preserve existing UI/UX and visual style
- ALWAYS follow existing folder structure
- ALWAYS prefer incremental edits
- ALWAYS reuse existing components before creating new ones
- ALWAYS preserve Supabase integration
- ALWAYS preserve Tailwind and shadcn/ui conventions
- ALWAYS inspect existing files before changing them

Project stack:
- React
- TypeScript
- Vite
- TailwindCSS
- shadcn/ui
- Supabase

UI style:
- Modern fintech dashboard
- Clean UX
- Dark sidebar
- Card-based layout
- Responsive
- Minimal and premium look

When modifying:
1. First inspect existing code
2. Explain planned changes
3. Modify only necessary files
4. Keep app working after every change
5. Never generate placeholder code